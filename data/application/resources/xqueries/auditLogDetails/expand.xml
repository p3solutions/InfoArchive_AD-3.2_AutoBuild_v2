<xquery xmlns="http://www.emc.com/documentum/xml/dds">
    <expression><![CDATA[
        
		import module namespace appname = 'appname' at '/APPLICATIONS/changeme/appname/appname.xqy';
		
		declare variable $path := concat("/DATA/",$appname:pathname,"/Collection/AuditTrail");
        declare variable $dateTime external;
        declare variable $user external;

        let $results :=
            <results> 
			{
				<result> 
				{
					for $elem in doc($path)/auditEntries/auditEntry[time = $dateTime and user = $user]/* return $elem
				}
				</result>
            } 
			</results>
        return $results
    ]]></expression>
</xquery>
